[telegram bot]
token =

[server]
ip =
# 443, 80, 88 or 8443 (port need to be 'open')
port = 8443
# In some VPS you may need to put here the IP addr
listen = 0.0.0.0

[ssl]
# Path to the ssl certificate
certificate = ./webhook_cert.pem
# Path to the ssl private key
private key = ./webhook_pkey.pem

[proxy]
# if you want to use proxy set True
enabled = False
# http or socks5
type = socks5
address =
port =
# for socks5 proxies only
user =
password =